import React from "react";
import stl from "./Footer.module.css";

const Footer = () => {
    return (
        <div>
            <h2 className={stl.footer}>Footer</h2>
        </div>
    );
}

export default Footer;